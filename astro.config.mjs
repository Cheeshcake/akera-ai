import { defineConfig } from 'astro/config';


// https://astro.build/config
export default defineConfig({
  site: "https://akeraai.netlify.app/",
	base: "/",
  devToolbar: {
    enabled: false
  }
});
